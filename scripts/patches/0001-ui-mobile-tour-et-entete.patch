diff --git a/web/frontend/src/components/page-header.tsx b/web/frontend/src/components/page-header.tsx
index 656551f3..aa6c4e33 100644
--- a/web/frontend/src/components/page-header.tsx
+++ b/web/frontend/src/components/page-header.tsx
@@ -20,20 +20,24 @@ export function PageHeader({
   return (
     <div
       className={cn(
-        "z-40 flex h-14 shrink-0 items-center justify-between px-6 pt-2",
+        "z-40 flex shrink-0 flex-col gap-2 px-6 pt-2 pb-2 sm:h-14 sm:flex-row sm:items-center sm:justify-between sm:gap-4 sm:pb-0",
         className,
       )}
     >
-      <div className="flex items-center gap-4">
+      <div className="flex min-w-0 items-center gap-4">
         <SidebarTrigger className="border-border/60 bg-background text-muted-foreground hover:bg-accent hover:text-foreground hidden h-9 w-9 rounded-lg border sm:flex [&>svg]:size-5">
           <IconMenu2 />
         </SidebarTrigger>
-        <h2 className="text-foreground/90 text-xl font-medium tracking-tight">
+        <h2 className="text-foreground/90 truncate text-xl font-medium tracking-tight">
           {title}
         </h2>
         {titleExtra}
       </div>
-      {children && <div className="flex items-center gap-2">{children}</div>}
+      {children && (
+        <div className="flex min-w-0 flex-wrap items-center gap-2 [&>div]:min-w-0 [&>div]:flex-wrap">
+          {children}
+        </div>
+      )}
     </div>
   )
 }
diff --git a/web/frontend/src/components/tour/tour-guide.tsx b/web/frontend/src/components/tour/tour-guide.tsx
index bd376109..6ef603e1 100644
--- a/web/frontend/src/components/tour/tour-guide.tsx
+++ b/web/frontend/src/components/tour/tour-guide.tsx
@@ -7,6 +7,7 @@ import { useAtom } from "jotai"
 import { useTranslation } from "react-i18next"
 
 import { Button } from "@/components/ui/button"
+import { useIsMobile } from "@/hooks/use-mobile"
 import { cn } from "@/lib/utils"
 import {
   type TourStep,
@@ -27,6 +28,7 @@ interface TourStepConfig {
 
 export function TourGuide() {
   const { t } = useTranslation()
+  const isMobile = useIsMobile()
   const [tourState] = useAtom(tourAtom)
   const [, setCurrentStep] = useAtom(tourCurrentStepAtom)
   const [, setIsActive] = useAtom(tourIsActiveAtom)
@@ -106,8 +108,13 @@ export function TourGuide() {
 
   const targetElement = getTargetElement()
 
+  // Sur un viewport etroit (WebView mobile), un ancrage `left`/`right` fait sortir le
+  // popover de l'ecran (titre et boutons coupes au bord gauche). On le centre alors,
+  // comme l'etape d'accueil, et on borne sa largeur a la fenetre.
+  const shouldCenter = isMobile || !targetElement
+
   const getPopoverPosition = () => {
-    if (!targetElement) {
+    if (shouldCenter || !targetElement) {
       return {
         top: "50%",
         left: "50%",
@@ -154,7 +161,7 @@ export function TourGuide() {
   }
 
   const position = getPopoverPosition()
-  const isCentered = !targetElement
+  const isCentered = shouldCenter
 
   return (
     <>
@@ -189,8 +196,8 @@ export function TourGuide() {
 
       <div
         className={cn(
-          "bg-background fixed z-[102] w-80 rounded-xl border p-4 shadow-2xl",
-          isCentered && "max-w-md",
+          "bg-background fixed z-[102] w-80 max-w-[calc(100vw-2rem)] rounded-xl border p-4 shadow-2xl",
+          isCentered && "max-w-[min(28rem,calc(100vw-2rem))]",
         )}
         style={position}
       >
